int pow(int x) {
    int a;
    int b;
    b = 5;
    outputInt(b);
    while (b < 30) {
        b = b + 1;
        if (b > 4) {
        continue;
        }
         b = b + 1;
    }
    outputInt(b);
    return 16 + 5 + x;
}

int main() {
    outputInt(pow(2));
    return 0;
}


